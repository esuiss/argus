ALTER TABLE authorization_codes
  ADD COLUMN resources text;

ALTER TABLE authorization_codes
  ADD CONSTRAINT authorization_codes_resources_length
    CHECK (resources IS NULL OR length(resources) <= 4096);

INSERT INTO argus_meta.schema_migrations (name) VALUES ('0010_code_resources');

DO $$
DECLARE offending text;
BEGIN
  SELECT string_agg(c.relname, ', ')
    INTO offending
    FROM pg_class c
    JOIN pg_namespace n ON n.oid = c.relnamespace
    JOIN pg_roles r ON r.oid = c.relowner
   WHERE n.nspname = 'public'
     AND c.relkind IN ('r', 'p')
     AND (NOT c.relrowsecurity OR NOT c.relforcerowsecurity OR r.rolsuper OR r.rolbypassrls);

  IF offending IS NOT NULL THEN
    RAISE EXCEPTION 'tables without RLS+FORCE or owned by a superuser: %', offending;
  END IF;
END
$$;
